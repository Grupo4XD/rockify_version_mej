name: proyecto_rockify
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.5

dependencies:
  cloud_firestore: ^6.5.0
  firebase_core: ^4.10.0
  flutter:
    sdk: flutter
  google_fonts: ^8.1.0
  http: ^1.6.0
  webview_flutter: ^4.13.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/imagenes/
